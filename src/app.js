const { sendMessagesToAll } = require('./services/messageScheduler');

// Manual trigger for testing (optional)
// sendMessagesToAll();

console.log('Islamic Education Messenger started. Scheduled tasks are running...');
